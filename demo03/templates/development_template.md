# 开发文档模板

> 使用说明：本文档由开发人员（或 AI 辅助）根据需求文档和 DWD/MID 层表结构分析后填写。
> 确认无误后，按本文档进行 SQL 开发。

---

## 一、需求来源

- **对应需求文档**：（需求文档名称或链接）
- **主题域**：（例：授信）
- **业务场景**：（例：授信全流程漏斗分析）

---

## 二、来源表分析

### DWD 层来源表

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| zijie.dwd_credit_apply_df | 授信申请明细表 | apply_id, user_id, apply_time, ds |
| （补充来源表） | | |

### MID 层来源表（如有）

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| | | |

### 表关联关系

| 左表 | 右表 | 关联键 | 关联方式 |
|------|------|-------|---------|
| dwd_credit_apply_df | dwd_credit_result_df | apply_id | LEFT JOIN |
| （补充关联） | | | |

---

## 三、DWS 层设计

### 表结构

- **表名**：zijie.dws_<主题域>_<业务描述>_<时间粒度>_df
- **说明**：
- **分区**：ds STRING（日期分区）
- **来源表**：

| 字段名 | 类型 | 说明 | 来源/计算逻辑 |
|-------|------|------|-------------|
| | | | |

### DDL

```sql
-- 在此填写 DWS 层建表语句
CREATE TABLE zijie.dws_xxx (
    ...
) COMMENT '...'
PARTITIONED BY (ds STRING COMMENT '日期分区')
STORED AS ORC;
```

---

## 四、ADS 层设计

### 日表

- **表名**：zijie.ads_<主题域>_<业务描述>_day_df
- **说明**：
- **来源表**：

| 字段名 | 类型 | 说明 | 来源/计算逻辑 |
|-------|------|------|-------------|
| | | | |

### 日表 DDL

```sql
-- 在此填写 ADS 日表建表语句
CREATE TABLE zijie.ads_xxx_day_df (
    ...
) COMMENT '...'
PARTITIONED BY (ds STRING COMMENT '日期分区')
STORED AS ORC;
```

### 周表（如需要）

- **表名**：zijie.ads_<主题域>_<业务描述>_week_df
- **说明**：
- **来源表**：

| 字段名 | 类型 | 说明 | 来源/计算逻辑 | 聚合方式 |
|-------|------|------|-------------|---------|
| | | | | SUM / 回源DWD |

### 周表 DDL

```sql
-- 在此填写 ADS 周表建表语句（如需要）
```

### 月表（如需要）

- **表名**：zijie.ads_<主题域>_<业务描述>_month_df
- **说明**：
- **来源表**：

| 字段名 | 类型 | 说明 | 来源/计算逻辑 | 聚合方式 |
|-------|------|------|-------------|---------|
| | | | | SUM / 回源DWD |

### 月表 DDL

```sql
-- 在此填写 ADS 月表建表语句（如需要）
```

---

## 五、指标口径明细

| 指标名称 | 字段性质 | 统计方式 | 所属层级 | 取值来源 | 取值规则 | 计算逻辑（SQL 表达式） | 展示格式 | 备注 |
|---------|---------|---------|---------|---------|---------|---------------------|---------|------|
| 日期 | 维度 | — | DWS/ADS | 来源表：`order_info`；字段：`create_date` | 取订单创建日期，按自然日截断 | ds | YYYY-MM-DD | |
| 申请数 | 原子指标 | 计数（去重） | DWS | 来源表：`credit_apply`；字段：`user_id` | 当日提交授信申请的去重用户数 | COUNT(DISTINCT user_id) WHERE ... | 整数 | 基础指标 |
| 授信通过率 | 派生指标 | 比率 | ADS | 来源表：`credit_apply`（同上） | 授信通过数 / 申请数 × 100% | pass_cnt / NULLIF(apply_cnt, 0) | 百分比（保留2位小数） | ADS 层计算，分母为0时展示"—" |
| （补充指标） | | | | | | | | |

---

## 六、待确认项

> 列出分析过程中发现的需要与产品确认的问题。

| 序号 | 问题描述 | 当前处理方式 | 确认状态 |
|-----|---------|------------|---------|
| 1 | （例：用户去重口径是按 user_id 还是 apply_id？） | 按 user_id 去重 | 待确认 |
| （补充） | | | |
